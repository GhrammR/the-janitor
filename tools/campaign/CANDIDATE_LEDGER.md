# Candidate Ledger

Valid findings from `janitor hunt` campaigns with an estimated Approval % between
10 and 84. These entries are not submission-ready yet: they are missing a fully
autonomous payload, require manual verification, or expose a mathematical proof
gap that must be closed before promotion to the Bounty Ledger.

| Date | Target Repo | Vulnerability Class | Severity | Expected Payout | Approval % | Exact Repro Command | R&D Follow-Up |
|------|-------------|---------------------|----------|-----------------|------------|---------------------|---------------|

| 2026-05-01 | mattermost/mattermost-plugin-boards | DOM XSS — webapp/src/utils.ts:143 | P2/Severe | $500–$1000 | 55% | HTML harness — `python3 -m http.server 8765` | Manual reflection proof is still required: trace channel or board-controlled data into the surviving HTML helper and confirm runtime conversion into a live DOM node. |
| 2026-05-01 | ClickHouse/ClickHouse | Unsafe string function — src/Functions/printf.cpp (×6 sprintf calls in SQL printf implementation) | P3/Medium | $100–$600 | 25% | Build ClickHouse from source with ASAN; execute SQL `SELECT printf('%s', repeat('A', 65536))` against a local test instance; observe ASAN stack trace | The mathematical gap is a missing bounded overflow witness proving attacker-controlled width reaches the intermediate formatting buffer without a guard. |
| 2026-05-01 | ClickHouse/ClickHouse | Raw pointer dereference — rust/workspace/prql/src/lib.rs (FFI boundary) | P4/Low | $50–$100 | 15% | Run `cargo test` in `rust/workspace/prql/` with MIRI enabled; observe UB on pointer deref | Manual verification must prove a public SQL PRQL path can drive attacker-controlled input into the unsafe FFI dereference. |
| 2026-05-02 | openai/codex | Intent Divergence — codex-rs/model-provider/src/auth.rs:58 UnauthenticatedAuthProvider skips all auth headers | P3/Medium | $500–$2000 | 40% | `grep -n UnauthenticatedAuthProvider /tmp/sprint94_codex/codex-rs/model-provider/src/auth.rs`; confirm reachable via non-test `ModelProviderInfo` with `requires_openai_auth = false` | The missing proof is endpoint elevation: demonstrate a sensitive context-forwarding path into an attacker-controlled provider configured with `requires_openai_auth = false`. |
| 2026-05-05 | openai/codex | Re-evaluation (Sprint 103) — UnauthenticatedAuthProvider finding confirmed present in current HEAD | P3/Medium | $500–$2000 | 40% | `grep -n UnauthenticatedAuthProvider /tmp/codex/codex-rs/model-provider/src/auth.rs` | No change. The capability gap remains a concrete proof that a hostile provider endpoint can receive sensitive context under a reachable unauthenticated configuration. |
| 2026-05-05 | smartcontractkit/chainlink | Unpinned asset / supply-chain drift — `deployment/ccip/shared/bindings/usd_stablecoin/usd_stablecoin_metadata.go:7` | Critical | up to $100,000 | 41% | `curl -fsSL "<remote-url>" -o /tmp/janitor_asset_probe && sha256sum /tmp/janitor_asset_probe` | Manual verification must prove the remote asset is mutable under a stable URL and can rotate without an immutable digest or checksum pin. |
| 2026-05-06 | smartcontractkit/chainlink | JWT validation bypass — `core/utils/jwt.go:230` | Critical | up to $100,000 | 36% | `curl -i -H 'Authorization: Bearer eyJhbGciOiJub25lIiwidHlwIjoiSldUIn0.eyJzdWIiOiJhdHRhY2tlciIsInJvbGUiOiJhZG1pbiJ9.' <protected-endpoint>` | Static token forgery evidence is concrete, but submission-grade proof still requires mapping a production ingress path into `core/utils/jwt.go` and confirming the vulnerable verifier is reachable in a supported deployment surface. |
| 2026-05-06 | smartcontractkit/chainlink | SQL injection — `core/store/store.go:156` concatenated query path | High | $5,000–$25,000 | 54% | `curl -s -X POST https://<target>/api/<endpoint> -H 'Content-Type: application/json' -d '{"input": "\\' OR \\'1\\'=\\'1"}'` | The IFDS proof is strong, but bounty promotion still requires one authenticated or unauthenticated API route whose user-controlled parameter demonstrably reaches `core/store/store.go:156` in the deployed Chainlink surface. |
| 2026-05-06 | https://github.com/freedomofpress/securedrop | Missing ownership checks on journalist/admin routes (`journalist_app/admin.py`, `journalist_app/col.py`, `journalist_app/main.py`) | P2/High | $1,000–$1,500 | 44% | Stand up the recommended prod-like SecureDrop environment, authenticate as a lower-privilege journalist principal, and replay `GET /<filesystem_id>/<fn>`, `GET /download_unread/<filesystem_id>`, `GET /edit/<int:user_id>`, `POST /delete/<int:user_id>`, and `POST /edit/<int:user_id>/new-password` with cross-user identifiers. | Prove one authenticated journalist or admin-adjacent principal can fetch or mutate another user's or source-linked record on the supported journalist interface; the current gap is a live authorization-boundary witness, not a missing static path proof. |
| 2026-05-08 | https://github.com/cashapp/misk | security:protobuf_any_type_field (`misk-proto/src/main/proto/misk/proto/status.proto:91`) | P3/Medium | $500–$5,000 | 35% | `python3 -c "import json; print(json.dumps({'@type':'type.googleapis.com/misk.proto.status.types.details','janitor_probe':'type-confusion-canary','role':'admin'}))"` — submit through any gRPC/HTTP JSON gateway endpoint that decodes status details | The detector confirmed `google.protobuf.Any` in the status details field; submission-grade proof requires tracing a reachable gRPC/HTTP decode path where an attacker-controlled `@type` URL reaches an unguarded `unpackTo` or authorization check without a closed type-url allowlist. [lattice-gap: P2-16] |

| 2026-05-08 | https://github.com/mattermost/mattermost | security:ssrf_dynamic_url (`server/channels/app/admin.go:161`) | P2/High | $500–$3,000 | 35% | `curl -s -X POST https://<mattermost>/api/v4/admin/<endpoint> -H 'Authorization: Bearer <admin-token>' -H 'Content-Type: application/json' -d '{"url": "http://169.254.169.254/latest/meta-data/"}'` | The sink is in server-side Go code reachable via the Mattermost REST API; the gap is proving an admin-accessible or user-accessible API endpoint that routes user-supplied URL into `admin.go:161` without a URL allowlist. Admin-only APIs require demonstrating the privilege is reachable from a lower-trust attacker path. |
| 2026-05-08 | https://github.com/mattermost/mattermost | security:unpinned_asset (`api/v4/html/ssr_template.hbs:9`) | P3/Medium | $200–$500 | 35% | `curl -s "$(grep -oP 'src="\K[^"]+' /tmp/mattermost/api/v4/html/ssr_template.hbs | head -1)" | sha256sum` — compare hash on two separate fetches to prove the asset is mutable under the same URL | SSR Handlebars template loads an external script without SRI pinning; submission requires proving the `<remote-url>` is a real mutable production CDN endpoint (not localhost), that the script path is rendered in a production Mattermost deploy, and that an attacker-controlled DNS or CDN compromise can substitute a payload script. |
| 2026-05-08 | https://github.com/mattermost/mattermost | security:tls_verification_bypass (`server/platform/shared/filestore/s3store.go:163` — `InsecureSkipVerify: true`) | P2/High | $500–$2,000 | 35% | `grep -n InsecureSkipVerify /tmp/mattermost/server/platform/shared/filestore/s3store.go` — confirm unconditional or config-flag-gated skip; run mitmproxy between Mattermost server and S3 endpoint with self-signed cert | The sink is in server-side Go code; gap is proving whether `InsecureSkipVerify` is admin-only config (approval < 10%) or a build-time default that is on in standard deployments (approval ~40%). Check `model/config/file_settings.go` for the config field default. |
