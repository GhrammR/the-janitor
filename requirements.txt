# Core Logic
tree-sitter>=0.22.0
tree-sitter-python
tree-sitter-javascript
tree-sitter-typescript
networkx
libcst

# CLI & UI
typer
rich
python-dotenv

# AI & Brain
# CRITICAL: Install CPU-only PyTorch BEFORE transformers to avoid CUDA bloat
# The --extra-index-url flag in Dockerfile will pull these from PyTorch CPU repo
torch>=2.0.0
torchvision>=0.15.0
chromadb>=0.6.0
transformers>=4.30.0
sentence-transformers
openai
tenacity

# Your Dev Tools (Remove before selling to enterprise)
google-genai
google-cloud-aiplatform

# Testing
pytest

# Documentation
mkdocs-material